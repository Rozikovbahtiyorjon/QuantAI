# =========================================================
# QuantAI - Runtime Dependencies
# Pinned to verified working versions (Python 3.12+).
# Regenerate pins after dependency changes: pip freeze (core only).
# =========================================================

# --- Core numeric / ML ---
numpy==2.5.1
pandas==3.0.5
pyarrow==25.0.1
scikit-learn==1.9.0
xgboost==3.3.0
joblib==1.5.3

# --- Configuration / CLI ---
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2
typer==0.27.1
rich==15.0.0

# --- Exchange connectivity ---
ccxt==4.5.68
aiohttp==3.14.1
websockets==16.1.1

# --- Used by experimental/frozen modules (monitoring, DR) ---
prometheus-client>=0.21
msgpack>=1.1
zstandard>=0.23

# --- Production guards & infrastructure ---
asyncpg==0.30.0
cryptography==45.0.4
redis==6.2.0

# --- ML Ensemble (optional, for HeterogeneousEnsemble) ---
catboost==1.3.4
lightgbm==4.6.0
scipy==1.15.2

# --- Telegram Office (natural language + LLM) ---
python-telegram-bot==21.9
openai==1.54.*

# NOTE (Phase 0 audit): removed as unused by any code path:
#   ta, python-binance, requests, matplotlib, PyYAML
