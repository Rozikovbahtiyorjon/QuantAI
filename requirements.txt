# =========================================================
# QuantAI - Runtime Dependencies
# Pinned to verified working versions (Python 3.12+).
# Regenerate pins after dependency changes: pip freeze (core only).
# =========================================================

# --- Core numeric / ML ---
numpy==2.5.1
pandas==3.0.5
pyarrow==25.0.1
scikit-learn==1.9.0
xgboost==3.3.0
joblib==1.5.3

# --- Configuration / CLI ---
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2
typer==0.27.1
rich==15.0.0

# --- Exchange connectivity ---
ccxt==4.5.68
aiohttp==3.14.1
websockets==16.1.1

# --- Used by experimental/frozen modules (monitoring, DR) ---
prometheus-client>=0.21
msgpack>=1.1
zstandard>=0.23

# NOTE (Phase 0 audit): removed as unused by any code path:
#   ta, python-binance, requests, matplotlib, PyYAML
